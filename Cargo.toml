[package]
name = "engram"
version = "0.1.0"
edition = "2024"
description = "Neural associative memory engine — attention-driven retrieval over compressed KV stores"
license = "MIT"

[dependencies]
thiserror = "2"
tracing = "0.1"

[dev-dependencies]
